# Núcleo de negocio (motor Python)
pydantic>=2.7
pydantic-settings>=2.2
pdfplumber>=0.11
openpyxl>=3.1
jinja2>=3.1
fpdf2>=2.7
pyyaml>=6.0

# API First (adaptador HTTP)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# --- Opcional: motor PDF de alta calidad (requiere GTK/Pango en el SO) ---
# Instalar con:  pip install -r requirements.txt weasyprint
# En Windows ver docs/RUNBOOK.md (instalación de GTK).
# weasyprint>=61.0

# --- Desarrollo / pruebas ---
pytest>=8.0
httpx>=0.27
ruff>=0.4
